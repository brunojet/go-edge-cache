IAM Role module

Purpose
- Create an IAM role intended for use by services such as Lambda.

Usage
- Set `create = true` to create the role.
- Provide `name`, optional `managed_policy_arns` and `inline_policy`.
